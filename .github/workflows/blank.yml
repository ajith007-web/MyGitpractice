name: myworkflow
on: 
  # push:
  #   branches: 
  #     - main
  # pull_request:
  #   branches:
  #     - main
  workflow_dispatch:
    inputs:
      name:
        description: 'Name of the person'
        required: true
        type: string
        default: 'John Doe'


jobs:
  # myjob:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: run checkout
  #       uses: actions/checkout@v7
  #     - name: run commands
  #       run: ls -lrt
  #     - name: run echo
  #       run: echo "actions created by ${{inputs.name}}"
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo "This is step 1"
  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: step 2
        run: echo "This is step 2"
  job3:
    runs-on: ubuntu-latest
    needs: job2
    steps:
      - name: step 3
        run: echo "This is step 3"
