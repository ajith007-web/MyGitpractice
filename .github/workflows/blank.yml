name: myworkflow
on: 
  # push:
  #   branches: 
  #     - main
  # pull_request:
  #   branches:
  #     - main
  workflow_dispatch:
    # inputs:
    #   name:
    #     description: 'Name of the person'
    #     required: true
    #     type: string
    #     default: 'John Doe'

env:
  MY_ENV_VAR: "This is an global environment variable"


jobs:
  # myjob:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: run checkout
  #       uses: actions/checkout@v7
  #     - name: run commands
  #       run: ls -lrt
  #     - name: run echo
  #       run: echo "actions created by ${{inputs.name}}"
  job1:
    runs-on: ubuntu-latest
    env:
      job_env_var: "This is a job1 specific environment variable"
    steps:
      - name: step 1
        run: echo "This is step 1"
      - name: step 2
        run: echo ${{env.MY_ENV_VAR}}
      - name: step 3
        run: echo ${{env.job_env_var}}
  job2:
    runs-on: ubuntu-latest
    env:
      job_env_var: "This is a job2 specific environment variable"
    needs: job1
    steps:
      - name: step 1
        run: echo ${{env.job_env_var}}
      - name: step 2
        run: echo ${{env.MY_ENV_VAR}}
  job3:
    runs-on: ubuntu-latest
    needs: job2
    steps:
      - name: step 3
        run: echo ${{env.MY_ENV_VAR}}
